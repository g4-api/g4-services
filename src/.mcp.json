{
	"servers": {
		"github-official": {
			"command": "docker",
			"args": [
				"run",
				"-i",
				"--rm",
				"--env-file",
				"${workspaceFolder}/.github.env",
				"ghcr.io/github/github-mcp-server"
			]
		}
	}
}