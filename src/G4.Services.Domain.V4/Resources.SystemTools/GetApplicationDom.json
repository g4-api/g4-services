{
	"description": "Retrieves the full HTML/XML markup of the application's Document Object Model (DOM) for the current browser session. Useful for inspecting or analyzing the current state of the loaded web page. The returned markup is optionally sanitized using a built-in normalization profile or delegated to an external OpenAI-compatible model. Neither the profile nor the openai properties are required; when omitted, the DOM is sanitized using the default interaction profile.",
	"inputSchema": {
		"type": "object",
		"properties": {
			"driverSession": {
				"description": "The unique session ID associated with the current browser session. This ID is used to retrieve the appropriate browser driver for interacting with the session and performing automation tasks.",
				"type": "string"
			},
			"openai": {
				"description": "When provided, delegates DOM sanitization to an external OpenAI-compatible model instead of the built-in sanitizer. The active profile value is forwarded to the model as part of the instruction context so the model can apply equivalent pruning intent. All sub-properties are optional within this object.",
				"type": "object",
				"properties": {
					"apiKey": {
						"description": "The API key used to authenticate requests to the OpenAI-compatible endpoint.",
						"type": "string"
					},
					"endpoint": {
						"description": "The base URL of the OpenAI-compatible API endpoint to which sanitization requests are sent.",
						"type": "string"
					},
					"headers": {
						"description": "Additional HTTP headers to include in every request sent to the endpoint. Expressed as a flat object of string key-value pairs. Useful for passing custom authorization, routing, or tracing headers required by the target API.",
						"type": "object",
						"additionalProperties": {
							"type": "string"
						}
					},
					"model": {
						"description": "The model identifier to use when sending the sanitization request to the endpoint, for example gpt-4o or a compatible deployment name.",
						"type": "string"
					}
				}
			},
			"profile": {
				"default": "interaction",
				"description": "Controls how the retrieved DOM markup is sanitized before it is returned. Accepted values are interaction, scraping, and none. interaction aggressively prunes hidden nodes, empty shells, noisy framework attributes, and collapses anonymous wrapper layers; optimized for XPath-based locator generation and is the default when this property is omitted. scraping applies lighter pruning, preserves hidden nodes, and retains more structural depth; suitable for content extraction workflows. none disables all sanitization and returns the raw DOM markup exactly as the browser produced it.",
				"enum": [
					"interaction",
					"scraping",
					"none"
				],
				"type": "string"
			},
			"token": {
				"description": "The G4 Authentication token used to authenticate the session initiation process. This is required to authorize the session creation.",
				"type": "string"
			}
		},
		"required": [
			"driverSession",
			"token"
		]
	},
	"name": "g4.GetApplicationDom",
	"outputSchema": {
		"properties": {
			"driverSession": {
				"description": "The session key for the browser session from which the DOM was retrieved.",
				"type": "string"
			},
			"value": {
				"description": "A string containing the HTML markup of the page's Document Object Model, sanitized according to the requested profile or the response produced by the external OpenAI-compatible model when the openai property was provided.",
				"type": "string"
			}
		},
		"required": [
			"driverSession",
			"value"
		],
		"type": "object"
	},
	"title": "Get Application DOM"
}
