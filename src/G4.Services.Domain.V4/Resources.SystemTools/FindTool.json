{
	"description": "Retrieves the complete G4 tool model for an exact tool name, including its authoritative namespace and client tool schema.",
	"inputSchema": {
		"properties": {
			"toolName": {
				"description": "The unique identifier of the tool to find.",
				"type": "string"
			}
		},
		"required": [
			"toolName"
		],
		"type": "object"
	},
	"name": "g4.FindTool",
	"outputSchema": {
		"properties": {
			"tool": {
				"description": "The complete G4 tool model, including its authoritative namespace and client tool definition.",
				"properties": {
					"clientTool": {
						"description": "The client-facing tool definition containing its title, description, and schemas.",
						"type": "object"
					},
					"description": {
						"description": "The human-readable description of the tool.",
						"type": "string"
					},
					"name": {
						"description": "The unique tool name.",
						"type": "string"
					},
					"namespace": {
						"description": "The authoritative namespace that owns the tool.",
						"type": "string"
					}
				},
				"required": [
					"clientTool",
					"name",
					"namespace"
				],
				"type": "object"
			}
		},
		"required": [
			"tool"
		],
		"type": "object"
	},
	"title": "Find G4 Tool"
}
